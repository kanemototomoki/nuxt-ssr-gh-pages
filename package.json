{
  "name": "nuxt-ssr-gh-page",
  "version": "1.0.0",
  "description": "My grand Nuxt.js project",
  "author": "kanemototomoki",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "run-s build && gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.0.0",
    "npm-run-all": "^4.1.5"
  }
}
